first of all...
Go to project folder...
cd "C:\Users\Muhammad Waseem\OneDrive\Desktop\ai_recommender\ai_career-recommender"

to create a virtual env...
 python -m venv venv

to install the dependancy ie. libraries need to train and run the model...
pip install -r requirements.txt 

and also check this commond...
pip install requirements

step two set the path  for the model in the test.py file and dataset csv path in the train.py file we you are going to train the model by yourself.

then check this commond...
python train.py

first test your model by running... 
python test.py 

then we can run this project using this commond...
python app.py